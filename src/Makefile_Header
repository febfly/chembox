FC := gfortran
FCFLAGS := -ffixed-line-length-none  -fbounds-check -fopenmp
#MODFLAG :=-J$(MODDIR)
LD := gfortran $(FCFLAGS)
LINK := 
#FCFLAGS := -ffixed-line-length-none 
#FC := ifort
#FCFLAGS := -extend-source -openmp -CB

FC90FLAGS:= -ffree-form -fbounds-check -fopenmp
	
